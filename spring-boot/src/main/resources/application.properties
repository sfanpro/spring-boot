info.app.name=Spring Boot Application
info.app.description=Very powerfull application!
info.app.version=1.0.0

# Use wildcard "*" to expose all endpoints
# can also expose individual endpoint with a comma-delimited list
management.endpoints.web.exposure.include=*

#Exclude individual endpoints with a comma-delimited list
management.endpoints.web.exposure.exclude=health,info

#Define my properties
coach.name=Super Mentor
team.name=The Best Club